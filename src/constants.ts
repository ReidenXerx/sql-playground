export const enum Colors {
  text = '#6E6E6F',
}

export enum RequestMethods {
  get = 'GET',
  post = 'POST',
  put = 'PUT',
  delete = 'DELETE',
}

export enum SourceDatabaseName {
  SQL = 'SQL',
  PVML = 'PVML',
}

export const apiUrl = 'api/v1/execute'
